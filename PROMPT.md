0a. familiarize yourself with the code in src/

read PLAN.md and implement the single highest priority feature - only work on a SINGLE feature

as you work update a local AGENTS.md in each directory with important learnings

ensure all tests and linting passes, then update PLAN.md with your progress

use git add -A and git commit -m "..." to commit your changes

git push origin main

if all tasks in the PLAN.md are finished, cancel the ralph loop
